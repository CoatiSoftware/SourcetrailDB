version: 0.0.0.{build}

image: 
    - Visual Studio 2017
    - Ubuntu
    - macos

platform: x64

configuration: Release

for:
-
    matrix:
        only:
            - image: Ubuntu
    build_script:
    - sh: echo running on Ubuntu!

build_script:
    - echo running!